import { SITE_URL } from "@/app/util/constants";

export const apiPlaygroundSeo = {
	title: "Query API — Interactive Explorer",
	description:
		"Run analytics queries, inspect response shapes, and copy ready-made requests from the Databuddy REST API. Privacy-first, typed, and free to start.",
	url: `${SITE_URL}/api`,
} as const;
